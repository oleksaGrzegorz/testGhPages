import styled from "styled-components";

const Button = styled.button`
  color: teal;
  background: white;
`;

function App() {
  return (
    <>
      <Button>Przycisk</Button>
      <Button>Przycisk</Button>
    </>
  );
}

export default App;
